{
    "require": {
        "symfony/phpunit-bridge": "^6.1"
    },
    "scripts": {
        "post-install-cmd": [
            "@php vendor/bin/simple-phpunit -h >/dev/null"
        ],
        "post-update-cmd": [
            "@php vendor/bin/simple-phpunit -h >/dev/null"
        ]
    }
}
